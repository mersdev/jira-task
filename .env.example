# Local development environment variables

# Docker registry token (optional)
DOCKER_TOKEN=

# Database (local or Podman network)
# Format: postgresql://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/devdb

# Rails secrets
# Run: rails secret in backend/ to generate
RAILS_MASTER_KEY=
SECRET_KEY_BASE=
