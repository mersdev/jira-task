name: Provision GCP Cloud Run

on:
  push:
    branches:
      - main
    paths:
      - 'infrastructure/terraform/**'
  workflow_dispatch:
    inputs:
      action:
        description: 'Terraform action to perform'
        required: true
        default: 'apply'
        type: choice
        options:
          - apply
          - plan
          - destroy

env:
  TERRAFORM_DIR: infrastructure/terraform

jobs:
  provision:
    name: Provision Infrastructure
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
      # Template reference: .github/template/provision-gcp-cloud-run/001-checkout-and-auth.yaml
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}

      # Template reference: .github/template/provision-gcp-cloud-run/002-terraform-apply.yaml
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.0

      - name: Initialize Terraform
        working-directory: ${{ env.TERRAFORM_DIR }}
        run: terraform init

      - name: Terraform ${{ github.event.inputs.action || 'apply' }}
        if: github.event_name == 'workflow_dispatch'
        working-directory: ${{ env.TERRAFORM_DIR }}
        run: |
          terraform ${{ github.event.inputs.action }} -var-file=terraform.tfvars.example -auto-approve

      - name: Terraform Plan
        if: github.event_name == 'push'
        working-directory: ${{ env.TERRAFORM_DIR }}
        run: terraform plan -var-file=terraform.tfvars.example

      - name: Terraform Apply
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        working-directory: ${{ env.TERRAFORM_DIR }}
        run: terraform apply -var-file=terraform.tfvars.example -auto-approve
